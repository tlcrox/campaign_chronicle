# Copy this file to .env and fill in the token.
#
# .env holds SECRETS ONLY. All tunables (model, language, scene detection,
# orchestration, etc.) live in config.yaml — see README.md ("Configuration").

# ---------- Diarization ----------
# Required ONLY when diarization is enabled (whisper.diarize: true in config.yaml).
# 1. Create a token at https://huggingface.co/settings/tokens (read scope is enough)
# 2. Accept terms for both models on Hugging Face:
#      https://huggingface.co/pyannote/speaker-diarization-3.1
#      https://huggingface.co/pyannote/segmentation-3.0
HF_TOKEN=
